---
name: generate-learning-cards
description: Produce structured learning-card candidates and review actions only.
---

Use only the supplied reading segment and annotation sources. Classify only word or phrase annotations; omit sentence annotations. Return exactly the supplied JSON schema. Never use tools, files, network, databases, or information outside the supplied candidates. For the second turn choose only the allowed action and only an existing item id supplied by the program.
