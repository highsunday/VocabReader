{
  "name": "codex-ai-chat-minimal-example",
  "version": "0.1.0",
  "private": true,
  "description": "Minimal Electron chat app backed by the local Codex app-server",
  "type": "module",
  "main": "dist/src/main/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "npm run build && electron .",
    "test": "npm run build && node --test dist/tests/*.test.js"
  },
  "devDependencies": {
    "@types/node": "^26.0.0",
    "electron": "^43.0.0",
    "typescript": "^6.0.0"
  }
}
