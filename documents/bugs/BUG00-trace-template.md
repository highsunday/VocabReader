---
bug-id: BUG-XX
title: <一句話描述症狀，例如「ArduPilot JSON 連線中斷後不自動重試」>
status: investigating  # investigating | resolved | wontfix
severity: medium       # critical | high | medium | low
created: <YYYY-MM-DD>
updated: <YYYY-MM-DD>
related-bxx: <修好後落地的 BXX 文檔編號，例如 B03；尚未產生時留空>
---

# BUG-XX 排查軌跡：<一句話症狀>

> 這份文件記錄「**怎麼找到根因**」的調查史，目的是讓排查方向只進不退地收斂——
> 任何接手的 AI 讀完後都能立刻知道：哪裡已經找過、哪裡還沒找、現在最該試什麼。
> 它**不是**最終修正規格；修好後請把根因＋修法＋回歸測試落到 `documents/implements/` 的 BXX。

## 狀態快照（接手者先讀這一段）

> 每次實驗後第一個更新這裡。目標：30 秒內讓任何 AI 知道現況與下一步。

- **現在相信的根因方向：** <一句話，沒有就寫「尚無假說」>
- **已排除（別再找）：** <一行摘要，例如「連線層、設定解析已排除」>
- **下一步要做什麼：** <具體到可執行，例如「檢查 reconnect() 的 backoff 是否吃掉例外」>
- **進度：** 已做 <N> 次實驗，目前最佳指標 = <X>

## 1. Bug 描述

<在什麼情況下、發生了什麼、不應該發生什麼。使用者影響是什麼。>

**錯誤指紋（用來辨識復發）：**

```
<精確錯誤訊息 / stack trace 關鍵行 / 異常數值>
```

## 2. 重現步驟

```bash
# 環境：OS / 語言版本 / 相依套件版本 / 相關設定
1. 執行 ...
2. 預期看到 X，實際看到 Y
```

出現頻率：穩定 / 間歇（約 X 次中 Y 次）/ 一次性
（間歇性時）疑似觸發條件：<例如：高負載 / 冷啟動 / 特定輸入>

## 3. 完成條件 (Done)

> 每個指標都要有「基準值」，否則無法判斷部分改善。

| 指標 | 基準值（現在） | 目標值（修好後） | 目前最佳 |
| ---- | -------------- | ---------------- | -------- |
| <pytest 通過率> | <3 / 10> | <10 / 10> | <3 / 10> |
| <啟動延遲> | <2.3s> | << 0.5s> | <2.3s> |

## 4. 已確認事實（建立後視為真，不再重測）

> 排查過程中被證實成立的不變量。寫進這裡的東西，後續 AI 不需要再驗證一次。

| 事實 | 如何被確認 | 日期 |
| ---- | ---------- | ---- |
| <設定檔有被正確讀入（已印出實際值核對）> | <EXP-001 加 log 確認> | <YYYY-MM-DD> |

## 5. 調查範圍

### 5.1 已排除 — 確認非原因

> 進入這張表必須有「排除依據」（一個觀測或一次實驗），不能只是直覺。
> 沒有新證據絕不重新調查這些範圍。

| 範圍 / 元件 | 排除依據 | 日期 |
| ----------- | -------- | ---- |
|  |  |  |

### 5.2 可疑點

| 優先 | 範圍 / 元件 | 懷疑理由 | 狀態 |
| ---- | ----------- | -------- | ---- |
| 🔴 |  |  | 未調查 |
| 🟡 |  |  | 未調查 |

### 5.3 未調查

- [ ] <尚未讀過的模組或流程>

## 6. 當前假說

<目前最合理的根因推測，以及支撐它的證據與反證。沒有線索時寫「尚無假說」。
每次實驗後更新：證據變強 / 變弱 / 被推翻。>

## 7. 實驗紀錄

> AI 自主執行，最新的在最上面。
> 結果：✅ 完全修復 / ⚠️ 部分改善 / ❌ 退步或無變化

### EXP-001 — <YYYY-MM-DD>

- **假說：** <根因推測>
- **修改內容：** <改了哪些檔案的哪些地方>
- **測試指令：** <執行了什麼>
- **指標變化：**
  - <pytest 通過率：3/10 → 6/10>
  - <啟動延遲：2.3s → 2.3s（無變化）>
- **結果：** ⚠️ 部分改善，修改已 commit（`<abc1234>`）
- **觀察 / 新線索：** <學到什麼、排除了什麼、下一步該往哪>

## 8. 解決方案

> 僅在 status = resolved 時填寫。填完後交棒 `ddd-doc` 將以下內容落到 BXX。

- **根因：**
- **修復方式：**
- **最終 commit 序列：**
- **回歸測試：** <新增了哪個測試確保此 bug 不再復發>
- **對應 BXX：** <落地的修正規格文檔編號>
